package user;
/*
This class checks whether:
- takes the user_id and checks to see if the inputted password matches it
- the inputted username matches a username in the database
- gets the user_id associate with that username
- returns the user_id
- also checks if the inputted username meets the size and character requirements
    - > 5 characters and <= 10 characters
    - no special characters
 */
public class Password {
}
